[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.localnet]
nft_staking = "8jV53ChBtfkidsc3k8LmyHn9xvqSZVM13qCvhCkFD99e"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

[test.validator]
# Lancer Surfpool à la place de solana-test-validator (MPL Core pré-déployé) :
#   Terminal 1: surfpool start --legacy-anchor-compatibility --watch
#   Terminal 2: anchor test --skip-local-validator
